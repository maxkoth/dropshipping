# Serves the static store bundle (local-store/) via Cloudflare Workers.
# Unique name so it never collides with other Workers in this account.
name = "linguaflow"
main = "worker.js"
compatibility_date = "2025-01-01"

[assets]
directory = "./local-store"
binding = "ASSETS"
