#:schema node_modules/wrangler/config-schema.json
# Serves the static store bundle (local-store/) as a Cloudflare Worker with
# static assets. No build step, no server code — Cloudflare just hosts the files.
name = "linguaflow"
compatibility_date = "2025-01-01"

[assets]
directory = "./local-store"
